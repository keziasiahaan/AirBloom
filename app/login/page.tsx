import { LoginForm } from "@/components/auth/login-form"

export const metadata = {
  title: "Login - AirBloom",
  description: "Masuk ke sistem monitoring kualitas udara",
}

export default function LoginPage() {
  return <LoginForm />
}
